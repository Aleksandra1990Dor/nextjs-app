'use client'

import { styled, Typography } from '@mui/material'

const LabelText = styled(Typography)({
	fontWeight: 600,
	marginBottom: '8px',
	fontSize: '14px',
	fontFamily: 'inherit'
})

export default LabelText
